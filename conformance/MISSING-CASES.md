# Missing Conformance Cases

Most of the original source-audited backlog is now implemented in
`conformance/cases/`.

The remaining backlog is intentionally small and now falls into one bucket.

## Oracle-specific Future Work

These only apply if the oracle shifts from upstream `OpenDoas` to the Alpine
packaged variant in [`.reference/Alpine-doas-3.23-stable`](/home/vince/Projects/rsudoas/.reference/Alpine-doas-3.23-stable).

- `config/confdir-ignores-legacy-doas-conf`
- `config/confdir-alphasort-order`
- `config/confdir-no-matching-files`
- `config/check-mode-confdir-path`
- `env/alpine-safe-path-order`

## Upstream Status

The currently tracked upstream `OpenDoas` backlog is implemented in
`conformance/cases/`.
